node_modules/
worker/node_modules/
worker/.wrangler/
.wrangler/
dist/

# Generated site output (built into dist/ only)
/index.html
/feed.xml
/robots.txt
/sitemap.xml
/changelog/
/now/
/posts/
/thinking/
/writing/
/reading/
/sharing/
.env*
!**/.env.example

# Build-time caches — now the D1 `build_cache` table (scripts/lib/build-cache.mjs).
# Kept on disk as a local fallback; never committed, since Pages builds in an
# ephemeral checkout and every write here would be discarded.
data/reading-covers.json
data/spotify-thumbnails.json
data/video-posters.json
data/linklog-unfurls.json

# OS / editor
.DS_Store
*.swp
*.swo
*~
.idea/
.vscode/

# npm/yarn failure logs
npm-debug.log*
yarn-error.log*

# Personal Claude Code tool-permission config, not project config
.claude/settings.local.json
